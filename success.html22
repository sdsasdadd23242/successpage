<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Subscription Success</title>
</head>
<body>
  <h1>Thank you for subscribing!</h1>
  <p>Your subscription is now active.</p>
  <button id="openExtension">Open Extension</button>
  <script>
    // Check if ?subscribed=true is in the URL
    const urlParams = new URLSearchParams(window.location.search);
    if (urlParams.get('subscribed') === 'true') {
      // Optionally set a localStorage flag
      localStorage.setItem('subscribed', 'true');
    }

    // When the button is clicked, open your extension's page
    document.getElementById('openExtension').addEventListener('click', () => {
      // Update this URL if your extension uses a different page
      window.location.href = 'chrome-extension://dpjbcalnafhafnckmdcjilikkfakcdpi/popup.html?subscribed=true';
    });
  </script>
</body>
</html>
